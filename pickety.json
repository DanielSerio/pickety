{
  "$schema": "./resources/pickety.schema.json",
  "modules": {
    "cli": "src/cli/*.ts",
    "commands": "src/commands/*.ts",
    "core": "src/core/*",
    "extension": "src/extension/*.ts",
    "services": "src/services/*.ts",
    "shared": "src/shared/*.ts",
    "vscode": "src/vscode/*.ts"
  },
  "rules": {
    "module-boundaries": {
      "severity": "error",
      "rules": [
        {
          "importer": "core",
          "imports": "core",
          "allow": true
        },
        {
          "importer": "core",
          "imports": "vscode",
          "allow": false,
          "message": "Core logic must remain platform-agnostic. No VS Code dependencies in src/core."
        },
        {
          "importer": "core",
          "imports": "services",
          "allow": false,
          "message": "Core logic must remain platform-agnostic. No VS Code dependencies in src/core."
        },
        {
          "importer": "vscode",
          "imports": "services",
          "allow": false,
          "message": "Infrastructure components should not depend on service orchestration."
        },
        {
          "importer": "extension",
          "imports": "services",
          "allow": true
        },
        {
          "importer": "extension",
          "imports": "vscode",
          "allow": true
        },
        {
          "importer": "cli",
          "imports": "core",
          "allow": true
        }
      ]
    }
  },
  "boundary-diagrams": true
}